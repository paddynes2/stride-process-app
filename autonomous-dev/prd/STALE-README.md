# STALE — Do Not Use

These files are from iterations 1-55 (pre-bootstrap). The live PRD files are at `prd/` in the project root.

Ralph reads/writes to `PROJECT_ROOT/prd/`, not `autonomous-dev/prd/`.

This directory is preserved for git history only.
