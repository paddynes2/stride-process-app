Done. Here's a summary of what was implemented:

**BUG-019** — `page.tsx` line 25: Changed `.select("*")` → `.select("*, users!activity_log_user_id_fkey(email)")`. This makes the server-side initial load use the same join as the API route, so user emails appear on first paint instead of "Unknown".

**IMP-018** — `activity-view.tsx`: Added guidance `<p className="text-[12px] text-[var(--text-tertiary)] mt-1">` below "No activity yet" in the `activeFilter === "all"` empty state: *"Actions like creating steps, adding comments, and completing runbooks appear here."*

**Verification**: typecheck ✅ (exit 0), lint ✅ (0 errors, 1 pre-existing warning in unowned file). Both files staged.
