**#FEAT-051 [2/2] complete.** Here's a summary of what was built:

---

**Task: FEAT-051 [2/2] — Coloring Rules UI**

**Files changed (6):**

1. **`src/types/canvas.ts`** — Added `ColoringTintContext` (`Map<stepId, hex>`) following the existing `CommentCountsContext`/`TaskCountsContext` pattern to avoid prop-drilling through the read-only `flow-canvas.tsx`.

2. **`src/components/canvas/step-node.tsx`** — Consumes `ColoringTintContext`; applies `backgroundColor: \`${color}26\`` (15% hex opacity) when heatmap mode is off and a rule matches. HeatMap tint takes precedence (unchanged logic).

3. **`src/components/canvas/coloring-panel.tsx`** (new) — Lightweight floating panel with:
   - Rule list: color swatch, name, `criteria_type: criteria_value`, active toggle (with CSS toggle animation), hover-reveal edit/delete buttons
   - Inline edit mode per rule
   - Add new rule form: name, color picker + hex input, criteria_type select, criteria_value input with hints

4. **`src/app/(app)/w/[workspaceId]/[tabId]/canvas-view.tsx`** — Fetches coloring rules on mount; evaluates rules in position order (last match wins) via `useMemo`; provides `ColoringTintContext`; renders paintbrush button overlay at `top-right` of canvas (away from react-flow toolbar at `top-left`).

5. **`src/app/api/v1/coloring-rules/route.ts`** — Added `HEX_COLOR_REGEX` validation on POST.

6. **`src/app/api/v1/coloring-rules/[id]/route.ts`** — Added `HEX_COLOR_REGEX` validation on color PATCH + `VALID_CRITERIA_TYPES` validation on criteria_type PATCH.

**Verification:** typecheck ✓ clean, lint ✓ no new warnings, build ✓ compiled successfully.
