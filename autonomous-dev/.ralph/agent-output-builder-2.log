Done. Here's what was changed:

**`handleStatusChange`** in `improvements-view.tsx` (lines 98-107):

1. `const prevIdeas = [...ideas]` — captures state before mutation
2. `setIdeas(prev => prev.map(...))` — applies optimistic update immediately
3. `await updateImprovementIdea(...)` — makes API call
4. On success: syncs with the server-returned object (catches any server-side field normalization)
5. On error: `setIdeas(prevIdeas)` reverts + `toastError` shows the failure message

Typecheck and lint both pass. 3 lines added, 0 removed.
