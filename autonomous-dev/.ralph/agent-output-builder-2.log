Done. Here's a summary of what was implemented for `#IMP-076`:

**Changes to `improvements-view.tsx`:**

1. **`formatTimeAgo` helper** — converts an ISO timestamp to "just now / Xm ago / Xh ago / Xd ago"

2. **`lastGeneratedAt` state** — initialized from `localStorage.getItem('stride:ai-suggestions:{workspaceId}:generated-at')` on mount (with SSR guard)

3. **Timestamp write on success** — `handleGenerateSuggestions` now saves `new Date().toISOString()` to localStorage and updates the state when suggestions load

4. **Label in header** — wraps the AI Suggestions button in a flex row; when `lastGeneratedAt` is set and not currently loading, shows `"Last generated Xm ago"` in `text-[11px] text-white/30` — matches the muted styling requirement

Verification: typecheck passes (0 errors), lint passes (0 errors, 1 pre-existing warning unrelated to this file).
