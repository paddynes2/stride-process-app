Task complete. Here's a summary of what was done:

**#IMP-053 — Prioritization empty state CTA**

**Changes made** to `src/app/(app)/w/[workspaceId]/prioritization/prioritization-view.tsx`:

1. Added `import Link from "next/link"` 
2. Modified the empty state `<div>` from `flex items-center justify-center` to `flex flex-col items-center justify-center gap-2` to support stacked content
3. Wrapped the existing message text in a `<span>`
4. Added a conditional `<Link>` component below the message:
   - Only renders when `tabs.length > 0` (hidden if no tabs)
   - Navigates to `/w/${workspaceId}/${tabs[0].id}` (first tab)
   - Styled `text-[var(--accent-blue)] hover:underline`

**Verification:** typecheck ✅ (0 errors), lint ✅ (0 errors, 1 pre-existing warning)
