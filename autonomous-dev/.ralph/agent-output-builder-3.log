Done. The change consolidates the two messages into one:

- **Removed:** Separate `<p>` link block below the card ("Open the canvas to set maturity scores →")
- **Changed:** Inline card text "Score steps to enable AI narrative." → linked sentence "Open the canvas to set maturity scores on steps to enable gap analysis and AI narrative →" (link on the canvas text, using `text-[var(--accent-blue)] hover:underline`)
- **Fallback:** If `firstProcessTab` is null, plain text without a link
- **No regression:** When `hasGapData` is true, the generate-summary text is unchanged

Typecheck: pass, Lint: pass (0 errors).
