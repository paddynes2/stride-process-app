Done. Summary of changes to `gap-analysis-view.tsx`:

**BUG-027 fix:**
- AI Narrative section moved outside `{hasGapData && (...)}` — always visible
- Generate Summary button disabled with muted styling + hint text "Score steps to enable AI narrative." when `!hasGapData`
- Button fully functional when `hasGapData` (no regression)

**IMP-066 (countdown timer):**
- Added `formatCountdown()` helper (M:SS format, copied from ai-analysis-view.tsx)
- Added `countdown` state + `lastTextRef` (restores previous narrative text when countdown expires)
- Added `useEffect` with `setInterval(1s)` — auto-transitions to `idle` at 0
- Rate-limited panel now shows `"Try again in M:SS"` instead of static minutes estimate
