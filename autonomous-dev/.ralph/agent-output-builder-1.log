Done. The change was minimal — one line in `tool-detail-panel.tsx:285`:

- **Before:** `"No steps linked to this tool."` with `text-[var(--text-tertiary)]`
- **After:** `"No steps linked yet. Open a step on the canvas and assign this tool."` with `text-white/30 italic`

Type-check and lint both pass. File staged.
