# STALE — Do Not Use

These files are from iterations 1-55 (pre-bootstrap). The live knowledge files are at `knowledge/` in the project root.

Ralph reads/writes to `PROJECT_ROOT/knowledge/`, not `autonomous-dev/knowledge/`.

This directory is preserved for git history only.
